<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.6.xsd">

    <changeSet id="CreateEmployeeTable" author="Stéphan.Minko">
        <createTable tableName="data.employees">
            <column name="UUID" type="varchar(255)">
                <constraints primaryKey="true"/>
            </column>
            <column name="EMAIL_ADDRESS" type="varchar(255)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="FIRST_NAME" type="varchar(255)">
                <constraints nullable="true"/>
            </column>
            <column name="LAST_NAME" type="varchar(255)">
                <constraints nullable="true"/>
            </column>
            <column name="BIRTHDAY" type="date">
            </column>
            <column name="DEPARTMENT_ID" type="bigint">
                <constraints nullable="false" foreignKeyName="FK_DEPARTMENT_ID" references="data.departments(ID)"/>
            </column>
            <column name="VERSION" type="bigint">
                <constraints nullable="false"/>
            </column>
        </createTable>
<!--                <sql>-->
<!--                    create table data.employees-->
<!--                    (-->
<!--                        uuid          varchar(255) not null constraint employees_pkey primary key,-->
<!--                        birthday      date,-->
<!--                        email_address varchar(255) constraint uk_email_address unique,-->
<!--                        first_name    varchar(255),-->
<!--                        last_name     varchar(255),-->
<!--                        version       bigint       not null,-->
<!--                        department_id bigint       not null constraint fk_department_id references departments-->
<!--                    );-->

<!--                    alter table data.employees owner to "user";-->
<!--                </sql>-->
    </changeSet>
</databaseChangeLog>