<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.6.xsd">

    <changeSet id="CreateOauthApprovalsTable" author="Stéphan.Minko">
        <sql>
            CREATE SEQUENCE oauth_approvals_sequence;
            create table oauth_approvals
            (
            id bigint not null DEFAULT NEXTVAL ('oauth_approvals_sequence') constraint oauth_approvals_pkey primary key,
            client_id varchar(255),
            expires_at timestamp,
            last_modified_at timestamp,
            scope varchar(255),
            status varchar(255),
            user_id varchar(255)
            );
        </sql>
    </changeSet>
</databaseChangeLog>